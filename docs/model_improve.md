We need the model to be defined in such a way that it can really count the user's good reps. 

We consider that leaving the model in a kind of idle state until it confirms that the user locks in a correct starting position for the exercise is key.

It should communicate to the user that they are not in correct position, and communicate when the user finally is, maybe be turning the points and lines green for a moment.

Then it should remember the coordinates of the points when the user is in a good starting position, to be able to count more effectively real reps. 

Let's try that.