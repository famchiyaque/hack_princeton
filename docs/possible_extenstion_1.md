Then warm the ElevenLabs cache — the bad voice will stop being what you notice.
Then add the LLM toggle, but scope it to post-rep reflection or end-of-set summary — not live per-frame.
Classifier ML model = cut for hackathon.